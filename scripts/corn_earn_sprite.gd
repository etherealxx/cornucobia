extends Sprite2D


## Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#var tween = create_tween()
	#tween.tween_property(self,
	#"position",
	#Vector2(804, 250),
	#1.0).from_current().set_ease(Tween.EASE_IN_OUT)
	#tween.tween_callback(func(): self.queue_free())
